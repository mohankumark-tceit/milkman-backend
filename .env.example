# Backend environment variables (copy to .env in backend/ for local dev)
# REQUIRED
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/milkman-app?retryWrites=true&w=majority
JWT_SECRET=change_this_to_a_random_long_secret
# Optional/for features
PORT=5001
# Comma-separated allowed origins for CORS (e.g. https://app.vercel.app,http://localhost:5173)
CORS_ORIGIN=http://localhost:5173
# Razorpay (optional — payments will be disabled if missing)
RAZORPAY_KEY_ID=rzp_test_xxx
RAZORPAY_SECRET=rzp_test_yyy
# SMTP (for OTP emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@example.com
SMTP_PASS=supersecret
